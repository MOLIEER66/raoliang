<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        M1 预留（PRD §7 Android 16 适配清单，M0 不声明、不加功能）：
        1. 前台服务媒体播放（Android 14+ 要求前台服务必须声明类型）：
           <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
           <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
           且 <service android:foregroundServiceType="mediaPlayback" ... />
        2. 通知权限（Android 13+）：
           <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
        3. 本地音频读取（Android 13+ 细分媒体权限）：
           <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    -->

    <application
        android:name=".EchoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Raoliang">

        <!-- 预测性返回（PRD §7）：迁移到 OnBackInvokedCallback，Android 16 上手势动画完整 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:enableOnBackInvokedCallback="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
