<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        权限与清单（M1-BREAKDOWN T1/T5 / ADR-0004 D1、D2）。
        T1 声明全部权限；T5 补上 PlaybackService 的 <service> 注册（媒体通知/锁屏控制、后台播放）。
    -->

    <!-- 本地音频读取：Android 13+ 细分媒体权限；音频无"部分访问"态，权限 UX 只有两态（ADR-0004 D2） -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- minSdk 26 兼容：26–32 仍用旧的读外部存储权限，33 起失效 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 通知权限（Android 13+ 运行时）：媒体通知的显示前提，未授予时服务不死、仅通知不显示（T5） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 后台播放前台服务（Android 14+ 强制声明类型，ADR-0004 D1；T5 起随 PlaybackService 生效） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:name=".EchoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Raoliang">

        <!-- 预测性返回（PRD §7）：迁移到 OnBackInvokedCallback，Android 16 上手势动画完整 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:enableOnBackInvokedCallback="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            T5 播放服务（ADR-0004 D1 方案 A）：MediaSessionService 标准路径。
            exported=true + intent-filter 是官方后台播放指南要求的会话发现入口；
            foregroundServiceType="mediaPlayback" 为 Android 14+ 强制声明（权限见上）。
        -->
        <service
            android:name=".core.playback.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>

</manifest>
